# 静的サイト軽量化・ビルド高速化計画

## 目的

Netlify 上の完全静的生成（`output: "export"`）を維持したまま、ビルド時間の安定化と公開物の削減を行う。外部 CDN の導入や SSR/ISR への移行は対象外とする。

## 現状ベースライン（2026-07-17）

- 記事数: 55 MDX
- `out/`: 約 29 MiB
- `out/blog/`（主に画像）: 約 17 MiB
- `out/_next/`: 約 4.7 MiB（ページデータは約 3.6 MiB）
- 外部 OGP を取得する `LinkCard`: 44 件
- `runFunction.ts` は毎回、公開画像を著者単位で削除して全コピーする

## 実施原則

- 各段階でビルド成功と表示互換性を確認してから次へ進む。
- 計測値と判断を本ファイルに追記する。
- 既存 URL、MDX の書式、静的書き出しを壊さない。
- 画像変換は毎回の Netlify ビルドではなく、画像更新時だけ行う。

## 実行環境の統一

- Node.js は 22 系を唯一の標準とする。
- `.nvmrc` はローカルで使用する Node 22.21.1 を指定する。`nvm use` はこのフォルダ内でだけ有効で、グローバル Node の既定値を変更しない。
- `.npmrc` の `engine-strict=true` により、Node 22 未満での依存関係の導入を失敗させる。
- Netlify は Node 22 を使用する。GitHub Actions の Node 19 は後続作業で Node 22 に統一する。

実施済み: ローカルの Node 22.21.1 で `npm run build` を実行し、`canvas` の ABI エラーなく 96 ページの静的生成を確認した。作業開始時の Node 20.19.3 はグローバル既定のまま変更していない。

## 段階

### Phase 0: 計測と再現性

- [x] ローカルNode 22で、メタデータ生成と静的出力のサイズを記録する。
- [x] `out/` を画像、HTML、JS、`_next/data` に分類して記録する。
- [x] `npm install` を lockfile 前提の `npm ci` に置き換える。

記録: `next/font/google` の外部取得を廃止後、コンパイルは 8.0 秒で成功した。ページデータ収集時に、ローカルの `remark-prism` 配下の `canvas` が Node ABI 127 で作られており、実行中の Node ABI 115 と不一致のため停止した。これは依存関係の再インストールまたは再ビルド後に再計測する（Netlify は Node 22 を指定済み）。

再計測（Node 22.21.1）: メタデータ生成は1.41秒。`out/` は約22.5 MiB、`out/blog/` は約15.5 MiB、`out/_next/` は約2.5 MiB、`out/_next/data/` は約1.6 MiB、HTML合計は約3.95 MiB、JavaScript合計は約0.88 MiB。作業開始時の `out/` 約29 MiB・ページデータ約3.6 MiBから縮小した。

完了条件: 以降の変更前後を同じ指標で比較できる。

### Phase 1: ビルド時の外部 OGP 取得を排除

- [ ] `LinkCard` の OGP 情報をローカルキャッシュファイルに保存する。
- [ ] 通常の Netlify ビルドはキャッシュのみ読み、外部 HTTP リクエストをしない。
- [ ] OGP を明示更新する専用コマンドを追加する。
- [ ] 未キャッシュの URL は現在のフォールバック表示を維持する。

実装済み: `posts/link-card-metadata.json` を通常ビルドの参照先にし、`npm run update:link-card-metadata` だけが外部 OGP を更新する。型チェックは成功。

品質保全: 外部取得を廃止する前の静的出力に残る `ogpMetadata` を `npm run seed:link-card-metadata` でキャッシュへ回収できる。通信失敗時にフォールバック値で既存の詳細なキャッシュを上書きしない。通常ビルドではネットワークを使わず、キャッシュ済みの情報を表示する。

検証: 58 URLをキャッシュし、57件で詳細情報、49件でカード画像を取得できた。`https://maximize.co.jp/442/innovation/` だけはタイムアウトのためフォールバック表示とする。

完了条件: 通常ビルドの所要時間と成否が外部サイトの応答に依存しない。

### Phase 2: 生成前処理の差分化

- [x] メタデータ生成、カバー生成、画像同期を責務別コマンドに分離する。
- [x] 画像同期を「削除して全コピー」から更新時刻・ファイルサイズベースの差分同期へ変える。
- [x] 生成物と投稿元の整合性を検証するコマンドを残す。

完了条件: 記事本文だけの更新で画像全体を再コピーしない。

検証: Node 22.21.1 で `runFunction.ts` を実行し、既存の120件の画像がすべて「変更なし」と判定され、コピー・削除は0件だった。

追加実装: 静的生成のプロセス内では、記事・著者・一覧カウントのJSONを一度だけ読み込む。ページごとの配列は新規に返すため、既存のソート処理に影響しない。

個別コマンド: `npm run generate:metadata` はJSONだけを更新し、`npm run generate:images` はカバー生成と差分同期だけを行う。`npm run verify:generated` はMDXとカバー画像の投稿元・公開先を検証する。従来どおり `npx ts-node runFunction.ts` は全処理を行う。

### Phase 3: ページデータの重複削減

- [x] 記事詳細の `relatedPosts` を同タグ優先・最大 5 件に制限する。
- [x] カード表示用の軽量データ型を導入し、本文・不要な項目を props に渡さない。
- [x] 新着・おすすめで同じ軽量型を使う。

完了条件: 記事数の増加に対する HTML と `/_next/data` の増加率を抑える。

実装済み: サイドバーには `id`、`title`、`category`、`updatedAt` のみを渡す。記事詳細のpropsから重複するカテゴリ・カバー画像・生成用メタデータを除外した。Node 22.21.1 で型チェック成功。本文が長い記事はMDXのシリアライズ結果自体が大半を占めるため、128 KB警告の解消はPhase 6で評価する。

### Phase 4: 画像の事前最適化

- [x] 最大サイズの PNG/JPEG を特定する。
- [ ] PNG/JPEGの形式は維持し、用途別（カバー・本文）の最大寸法を決める。
- [ ] 大きすぎる画像だけを投稿時に個別に縮小・圧縮し、既存の参照先を維持する。
- [ ] 形式変換や自動画像変換の仕組みは導入しない。

完了条件: 見た目を維持しつつ `out/blog/` を削減する。

調査結果: 投稿画像は合計約15.3 MiB。最大は `igem-japan-journal-club-epfl2019-fig7.png`（2.24 MiB、1730×1564px）で、上位10画像だけで約6.4 MiBを占める。形式は変えず、編集上必要な画像だけを個別に見直す。

### Phase 5: 初回 JavaScript の削減

- [x] 目次生成を必要時に遅延読込にする。
- [x] 不要な内部 Next.js import を除去する。
- [ ] 記事ページの初回 JS サイズを変更前後で比較する。

実装済み: `tocbot` は記事のクライアントマウント後に読み込む。共有ボタンは初期HTMLに残し、操作可能な状態で表示する。SNS埋め込みは表示互換性を優先して現状のままとする。

計測: 共有ボタンを遅延化すると約9.8 KiBを分離できたが、初期表示でボタンが消えるため不採用とした。

品質確認: 画像ファイルは変更していない。静的生成後の記事HTMLには共有用のボタンが3つ含まれることを確認した。目次は従来どおりクライアントマウント後に生成される。

完了条件: 記事閲覧に不要な JavaScript を初回ロードしない。

### Phase 6: 必要時のみ行う MDX 描画方式の見直し

- [ ] ページデータが警告値を超える記事は、必要に応じて編集上「前編・後編」の独立したMDX記事として投稿する。自動分割などの仕組みは作らない。
- [ ] 前編・後編の本文には相互リンクを置き、既存記事を分割する場合だけ旧URLの扱いを個別に決める。
- [ ] Phase 3 までで不十分な場合のみ、MDX のシリアライズ props を静的 HTML 化する案を試作する。
- [ ] カスタム MDX コンポーネント、埋め込み、目次との互換性を確認する。

完了条件: 記事分割または描画方式の変更のうち、効果が移行コストを上回るものだけを採用する。

候補: 現在のビルドで `/tech/slack-biorxiv-synbio/` がページデータ129 KBの警告対象。自動処理ではなく、内容に応じた前編・後編の編集対象として判断する。

## 直近の作業

1. 大容量記事を前編・後編へ分けるか、編集担当が判断する。
2. 大きすぎるPNG/JPEGを個別に見直す場合は、原寸画像と記事内での可読性を比較してから更新する。
3. Netlifyの実ビルド時間は次回デプロイのログで記録する。
